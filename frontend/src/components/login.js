import React from 'react';

const Login = props => {

    return(
        <header className='App-header'>
            <h1>Login</h1>
            <h3>login stuff...</h3>
        </header>
    );
}

export default Login;